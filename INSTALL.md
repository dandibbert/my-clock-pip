# 安装悬刻

构建产物 `MyClockPiP-unsigned.ipa` 是已编译的 iPhone/iPad 应用，不需要在本地编译源码。

**这是未签名 IPA，不是可以直接点击安装的 App Store/TestFlight 包。** 普通 iOS 设备必须经过有效的开发/分发或侧载签名后安装。请使用自己已有的可信签名与安装方式；开发者签名可能有设备注册、开发者模式和有效期要求。仓库没有也不需要保存 Apple ID、证书或密码。

要求：iOS/iPadOS 16.0 及以上、系统支持画中画。模拟器截图和自动测试不能代替实机后台画中画测试。

## 开始使用

1. 打开悬刻；默认显示北京时间，点「立即校时」可进行网络参考校时。
2. 按需设置开抢时间，然后点「开启悬浮时钟」，等小窗出现再切换 App。
3. 双指张开/捏合调整小窗大小；拖动到屏幕角落。主界面另有内容字号和画面比例设置。

## 注意

- 三位毫秒不是 ±1 ms 准确度保证。60 FPS 目标帧间隔约 16.7 ms，实际受系统限制。
- 网络校时有效期 15 分钟；过期退回设备时间并标注。开抢前重新校时；不要在最后几秒突然修改时间源或偏移。
- SNTP 校时失败不影响设备时钟使用；UDP/123 可能被网络/VPN 阻止。
- 不要从多任务界面划掉本应用。来电、锁屏、其他画中画视频或系统资源回收可能中断。
- 系统暂停后显示「已暂停」，点播放可恢复；本应用不执行购物、付款或自动点击。
